// Declarando duas variáveis inteiras
let a = 10
let b = 5

// Soma das variáveis
let soma = a + b

// Verificando se é divisível por 2
if soma % 2 = 0 then
    printfn "A soma (%d) é divisível por 2." soma
else
    printfn "A soma (%d) NÃO é divisível por 2." soma
